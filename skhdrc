#!/usr/bin/env sh

# YabaDuma - SKHD Configuration
# Keyboard shortcuts for yabai window management

# ============================================
# Application Launches
# ============================================

# Terminal
alt - q : open -na /Applications/kitty.app

# Browser
cmd - b : open -a /Applications/Zen.app

# Code Editor
alt - c : open -na /Applications/Visual\ Studio\ Code.app

# Spotify
alt - s : open -a /Applications/Spotify.app

# Finder
cmd - return : open -a /System/Applications/Finder.app

# ============================================
# Window Navigation
# ============================================

# Focus window in direction
alt - left : yabai -m window --focus west
alt - down : yabai -m window --focus south
alt - up : yabai -m window --focus north
alt - right : yabai -m window --focus east

# ============================================
# Window Movement / Swapping
# ============================================

# Smart swap (with split toggle fallback)
shift + alt - left : ~/.config/skhd/scripts/smart-swap.sh west
shift + alt - right : ~/.config/skhd/scripts/smart-swap.sh east
shift + alt - up : ~/.config/skhd/scripts/smart-swap.sh north
shift + alt - down : ~/.config/skhd/scripts/smart-swap.sh south

# ============================================
# Workspace Navigation
# ============================================

# Move window to previous/next workspace and follow
shift + ctrl - left : yabai -m window --space prev; yabai -m space --focus prev
shift + ctrl - right : yabai -m window --space next; yabai -m space --focus next

# ============================================
# Window Resizing
# ============================================

# Resize windows using helper script
ctrl + alt - left : ~/.config/skhd/scripts/resize-window.sh left
ctrl + alt - down : ~/.config/skhd/scripts/resize-window.sh down
ctrl + alt - up : ~/.config/skhd/scripts/resize-window.sh up
ctrl + alt - right : ~/.config/skhd/scripts/resize-window.sh right

# ============================================
# Window State
# ============================================

# Float / Unfloat window (centered grid)
ctrl - z : yabai -m window --toggle float && yabai -m window --grid 4:4:1:1:2:2

# Make window fullscreen
shift + alt - m : yabai -m window --toggle zoom-fullscreen

# Toggle split orientation
alt - o : yabai -m window --toggle split

# ============================================
# Space Layout
# ============================================

# Rotate windows clockwise and anticlockwise
ctrl + alt - r : yabai -m space --rotate 270
ctrl + alt + shift - r : yabai -m space --rotate 90

# Balance out all windows
ctrl + alt - b : yabai -m space --balance

# Change layout (using ctrl + alt + number keys)
ctrl + alt - 1 : yabai -m space --layout bsp
ctrl + alt - 2 : yabai -m space --layout stack
ctrl + alt - 3 : yabai -m space --layout float

# ============================================
# Service Management
# ============================================

# Restart yabai
ctrl + alt + shift - 0 : yabai --restart-service
